# Server Configuration
PORT=3000
APP_BASE_URL=http://localhost:4000
# Comma-separated browser origins allowed to call the API (required for Netlify → Render)
# Example: https://fooddelivery93.netlify.app,http://localhost:5173
CLIENT_ORIGINS=

# Seed: demo (10 restaurants, fast) | full (1000 restaurants, local only)
SEED_PROFILE=demo
SEED_IF_EMPTY=true
API_VERSION=v1
NODE_ENV=development
ENVIRONMENT=dev

# JWT Secrets
JWT_SECRET=b291703a2eeccba9d5b6a57d2f982b257c43ab6380226d030e17532bac3eb4a7
ACCESS_TOKEN_SECRET=c4fbfd003608af8776a4fa19e6b11149bbd25a1d76e63aecf01c904b6b1836a8
REFRESH_TOKEN_SECRET=faa5edc3b933a7be1202ee80fd2e43f2fd89d6b3597c74e156a3c86fc1cca362

# Token Expiry
ACCESS_TOKEN_EXPIRY=30d
REFRESH_TOKEN_EXPIRY=90d

# Database
DATABASE_URL=postgresql://postgres:12345@localhost:5433/food-delivery?schema=public&connection_limit=50&pool_timeout=20

DATABASE_URL=postgresql://${DATABASE_USER}:${DATABASE_PASSWORD}@localhost:${DATABASE_LOCAL_PORT}/${DATABASE_NAME}?schema=public
DATABASE_PORT=5432
DATABASE_LOCAL_PORT=5433
DATABASE_NAME=food-delivery
DATABASE_USER=postgres
DATABASE_PASSWORD=test

# Password Reset
PASSWORD_RESET_TOKEN_EXPIRY=3600000  # 1 hour in milliseconds
PASSWORD_RESET_URL=http://localhost:3000/auth/reset-password  # Frontend URL for reset page

# Verify Email
EMAIL_VERIFICATION_URL=http://localhost:3000/auth/verify-email  # Frontend URL for email verification
EMAIL_VERIFICATION_TOKEN_EXPIRY=86400000  # 24 hours in milliseconds

# Email Service (Optional - will log to console if not configured)
# Enable 2FA and generate app password at: https://myaccount.google.com/apppasswords
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE_PORT=465
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
EMAIL_FROM=noreply@yourapp.com

# Alternative Email Providers:
# SendGrid:
# EMAIL_HOST=smtp.sendgrid.net
# EMAIL_PORT=587
# EMAIL_USER=apikey
# EMAIL_PASSWORD=your-sendgrid-api-key

# Mailgun:
# EMAIL_HOST=smtp.mailgun.org
# EMAIL_PORT=587
# EMAIL_USER=your-mailgun-smtp-username
# EMAIL_PASSWORD=your-mailgun-smtp-password

# Security
SALT_ROUNDS=10

STRIPE_SECRET_KEY=sk_test_...
STRIPE_PUBLISHABLE_KEY=pk_test_...

# Run: stripe listen --forward-to localhost:4000/webhooks/stripe
# Copy the whsec_... value printed EACH TIME you start "stripe listen" and paste it below.
# If this secret does not match, webhooks fail and orders stay PENDING (storefront also calls POST /orders/confirm-payment as backup).
STRIPE_WEBHOOK_SECRET=whsec_...
